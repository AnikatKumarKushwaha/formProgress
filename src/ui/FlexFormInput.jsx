/* eslint-disable react/prop-types */
const FlexFormInput = ({ children }) => {
  return (
    <div className="flex justify-center gap-10 items-center">{children}</div>
  );
};

export default FlexFormInput;
